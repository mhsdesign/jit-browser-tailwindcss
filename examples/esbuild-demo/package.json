{
  "name": "esbuild-demo",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "rm -rf ./dist && node build.js"
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5.9",
    "jit-browser-tailwindcss": "file:../..",
    "esbuild": "^0.15.0"
  }
}
